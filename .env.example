# Copy to .env.local for local development and fill in your Supabase project's
# values (Project Settings → API). Without these, the app runs exactly as
# before: local-only, per-browser storage, no shared database.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
